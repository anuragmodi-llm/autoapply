{
  "name": "autoapply-backend",
  "version": "1.0.0",
  "description": "Minimal backend proxy for AutoApply — keeps LLM API keys secret",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "dotenv": "^16.5.0",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3"
  }
}
